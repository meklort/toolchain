osx_image: xcode6.4
language: c++

matrix:
  include:
    #- os: osx
    #  osx_image: xcode6.4
    - os: linux

addons:
  apt:
    packages:
      - cmake
      - xz-utils
      - tar
      - libudev-dev
      - texinfo


script:
  - make
  - make cpack
